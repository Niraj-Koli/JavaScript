const summation = (n1, n2) => n1 + n2;

const deduction = (n1, n2) => n1 - n2;

const accumulation = (n1, n2) => n1 * n2;

const splitting = (n1, n2) => n1 / n2;

export default summation;
export { deduction, accumulation, splitting };
