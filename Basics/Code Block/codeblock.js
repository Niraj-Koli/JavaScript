const age = 20;
const person = "Kaizer";

if (true) {
    const age = 40;
    const person = "Dante";

    console.log(`${person} is ${age} old.`);

    if (true) {
        const age = 30;
        const person = "Hertz";

        console.log(`${person} is ${age} old.`);
    }
}

console.log(`${person} is ${age} old.`);
